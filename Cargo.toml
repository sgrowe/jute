[package]
name = "jute"
version = "0.1.0"
edition = "2024"

# Optimise for smaller build size
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dependencies]
